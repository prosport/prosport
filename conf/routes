# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET         /                     controllers.Application.index()

# Authentication
GET         /login                controllers.Application.login()
POST        /login                controllers.Application.authenticate()
GET         /logout               controllers.Application.logout()

#tree
GET         /tree                 controllers.TreePageController.GET()


#catalog
GET         /catalog              controllers.CatalogPageController.catalog(name:String = "Каталог")
GET         /catalog/:name        controllers.CatalogPageController.catalog(name:String)

#product details
GET         /product/:productId    controllers.ProductDetailsPageController.getProductDetailsPage(productId:Long)

->          /admin                sa.Routes

# Test Admin Templates
GET 		/admin2				  controllers.AdminController.index()
GET 		/admin2/categories	  controllers.AdminController.categories()
GET 		/admin2/pages	  	  controllers.AdminController.pages()
GET 		/admin2/media	  	  controllers.AdminController.media()

# Map static resources from the /public folder to the /assets URL path
GET         /assets/*file         controllers.Assets.at(path="/public", file)
GET         /images/*file         controllers.ExternalAssets.at(path="media", file)
